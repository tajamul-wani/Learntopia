# Local-only instruction / reference docs (never published)
TESTING.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files
.env
.env.local
.env.*
!.env.example

# Agent instruction and configuration files
.agents
.agents/
.claude/
skills-lock.json

# Local-only control documents and session history. COLLAB_PROTOCOL.md and
# claude-session/ live in the parent folder, outside this repo; these entries
# exist so a stray copy can never be committed.
COLLAB_PROTOCOL.md
claude-session/

# Wrangler build output (the Cloudflare Workers in worker/ and worker-sentry-linear/)
.wrangler/

# Editor and tooling backups
*.bak

# Private Product Roadmap & Suggestions Tracking
PRODUCT_ROADMAP_SUGGESTIONS.md

# Test output
coverage/
.vitest/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
